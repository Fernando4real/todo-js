//Importacion de todas las clases a un mismo archivo parra solo importar una linea de archivo al index

import {Todo} from './todo.class'
import {TodoList} from './todo-list.class'

export{
  Todo,
  TodoList
}